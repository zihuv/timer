import SwiftUI

/// 内容视图
/// 注意：此应用是菜单栏应用，不需要主窗口
/// 此文件保留仅为满足 Xcode 项目模板要求
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
